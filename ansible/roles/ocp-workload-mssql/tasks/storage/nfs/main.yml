---
- name: "{{ title }} NFS storage"
  k8s:
    state: "{{ state }}"
    definition: "{{ lookup('template', item) }}"
  with_items:
    - "storage/{{ mssql_pv_provider }}/pv.yaml.j2"
    - "storage/{{ mssql_pv_provider }}/pvc.yaml.j2"